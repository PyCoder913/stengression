# .readthedocs.yaml

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: docs/conf.py

python:
  install:
    # Install actual package
    - method: pip
      path: .
      
    # Install Sphinx theme and extensions
    - requirements: docs/requirements.txt
